
void setup(){
  size(500,500);

}
 void draw(){
   background(123,mouseX, mouseY); 
   
   rectMode(CENTER);
   ellipse(mouseY, mouseX, 250,250);

   rectMode(CENTER);
   ellipse(mouseX,mouseY, 240,240);
   
   rectMode(CENTER);
   ellipse( mouseY, mouseX, 230,230);
  
   rectMode(CENTER);
   ellipse(mouseX, mouseY, 220,220);
   
   rectMode(CENTER);
   ellipse(mouseY, mouseX, 210,210);
   
   rectMode(CENTER);
   ellipse(mouseX, mouseY, 200,200);
   
   rectMode(CENTER);
   ellipse(mouseY, mouseX, 190,190);
   
   rectMode(CENTER);
   ellipse(mouseX, mouseY, 180,180);
   
   rectMode(CENTER);
   ellipse(mouseY, mouseX, 170,170);
   
   rectMode(CENTER);
   ellipse(mouseX, mouseY, 160,160);
   
   rectMode(CENTER);
   ellipse(mouseY, mouseX, 150,150);
   
   rectMode(CENTER);
   ellipse(mouseX, mouseY, 140,140);
   
   rectMode(CENTER);
   ellipse(mouseY, mouseX, 130,130);
   
   rectMode(CENTER);
   ellipse(mouseX, mouseY, 120,120);
   
   rectMode(CENTER);
   ellipse(mouseY, mouseX, 110,110);
   
   rectMode(CENTER);
   ellipse(mouseX, mouseY, 100,100);
   
   rectMode(CENTER);
   ellipse(mouseY, mouseX, 90,90);
   
   rectMode(CENTER);
   ellipse(mouseX, mouseY, 80,80);
   
   rectMode(CENTER);
   ellipse(mouseY, mouseX, 70,70);
   
   rectMode(CENTER);
   ellipse(mouseX, mouseY, 60,60);
   
   rectMode(CENTER);
   ellipse(mouseY, mouseX, 50,50);
   
   rectMode(CENTER);
   ellipse(mouseX, mouseY, 40,40);
   
   rectMode(CENTER);
   ellipse(mouseY, mouseX, 30,30);
   
   rectMode(CENTER);
   ellipse(mouseX, mouseY, 20,20);
   
   rectMode(CENTER);
   ellipse(mouseY, mouseX, 10,10);
   
   rectMode(CENTER);
   ellipse(mouseX, mouseY, 2,2);
   
   rectMode(CENTER);
   ellipse(mouseY, mouseX, 1,1);
 }
