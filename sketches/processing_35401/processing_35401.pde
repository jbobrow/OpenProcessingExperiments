
// This is a Practice Sketch 
// Jacob Wan 
int loop_counter=0;
void setup  () {
  size (800, 600);
  strokeWeight (4);
  frameRate (10);
  background (180, 200, 190, 10); 
  smooth();
} 

void draw () {
   loop_counter++; 
  int centerX = floor(width/2); 
  int centerY = floor(height/2);
  noStroke();
  fill(random (30), random(30), random(30),10);
  rotate (random(TWO_PI));
  ellipse(centerX,centerY, random(400), random(400));
  fill(180,200,190,10);
  text("iteration="+loop_counter, width-100, height-10);
  
}               

























































































































































































































































































































































































































































































































































































































































































































































































































































