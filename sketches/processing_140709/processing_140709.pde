
void setup() {
  size(500,500);
  background(0);
}

void draw() {
  noStroke();
  
  int a = 1000;
   
  if ( millis() > a && millis() < 2*a  ) {  
  
  int i = 20;
  while ( i <= 600 ) {  // outter
    int j = 20;
    while ( j <= 160 ) {  // inner
      fill(i,100,250,100);
      rect(j,i, 20,20);      
      j = j + 40;
    }
    i = i + 40;
  }

  }
  
    if ( millis() > 2*a && millis() < 3*a  ) {  
  
  int i = 20;
  while ( i <= 600 ) {  // outter
    int j = 180;
    while ( j <= 320 ) {  // inner
      fill(i,100,250,100);
      rect(j,i, 20,20);      
      j = j + 40;
    }
    i = i + 40;
  }

  }
  
 if ( millis() > 3*a && millis() < 4*a  ) {  
  
  int i = 20;
  while ( i <= 600 ) {  // outter
    int j = 340;
    while ( j <= 480 ) {  // inner
      fill(i,100,250,100);
      rect(j,i, 20,20);      
      j = j + 40;
    }
    i = i + 40;
  }

  }

  
   if ( millis() > 4*a && millis() < 5*a  ) {  
  
  int i = 20;
  while ( i <= 600 ) {  // outter
    int j = 20;
    while ( j <= 160 ) {  // inner
      fill(i,100,250,100);
      rect(j-20,i, 20,20);      
      j = j + 40;
    }
    i = i + 40;
  }

  }
  
    if ( millis() > 5*a && millis() < 6*a  ) {  
  
  int i = 20;
  while ( i <= 600 ) {  // outter
    int j = 180;
    while ( j <= 320 ) {  // inner
      fill(i,100,250,100);
      rect(j-20,i, 20,20);      
      j = j + 40;
    }
    i = i + 40;
  }

  }
  
 if ( millis() > 6*a && millis() < 7*a  ) {  
  
  int i = 20;
  while ( i <= 600 ) {  // outter
    int j = 340;
    while ( j <= 600 ) {  // inner
      fill(i,100,250,100);
      rect(j-20,i, 20,20);      
      j = j + 40;
    }
    i = i + 40;
  }

  }
  
    if ( millis() > 7*a && millis() < 8*a  ) {  
  
  int i = 20;
  while ( i <= 600 ) {  // outter
    int j = 20;
    while ( j <= 160 ) {  // inner
      fill(i,100,250,100);
      rect(j,i-20, 20,20);      
      j = j + 40;
    }
    i = i + 40;
  }

  }
  
    if ( millis() > 8*a && millis() < 9*a  ) {  
  
  int i = 20;
  while ( i <= 600 ) {  // outter
    int j = 180;
    while ( j <= 320 ) {  // inner
      fill(i,100,250,100);
      rect(j,i-20, 20,20);      
      j = j + 40;
    }
    i = i + 40;
  }

  }
  
 if ( millis() > 9*a && millis() < 10*a  ) {  
  
  int i = 20;
  while ( i <= 600 ) {  // outter
    int j = 340;
    while ( j <= 600 ) {  // inner
      fill(i,100,250,100);
      rect(j,i-20, 20,20);      
      j = j + 40;
    }
    i = i + 40;
  }

  }

  if ( millis() > 10*a && millis() < 11*a  ) {  
  
  int i = 20;
  while ( i <= 600 ) {  // outter
    int j = 20;
    while ( j <= 160 ) {  // inner
      fill(i,100,250,100);
      rect(j-20,i-20, 20,20);      
      j = j + 40;
    }
    i = i + 40;
  }

  }
  
    if ( millis() > 11*a && millis() < 12*a  ) {  
  
  int i = 20;
  while ( i <= 600 ) {  // outter
    int j = 180;
    while ( j <= 320 ) {  // inner
      fill(i,100,250,100);
      rect(j-20,i-20, 20,20);      
      j = j + 40;
    }
    i = i + 40;
  }

  }
  
 if ( millis() > 12*a && millis() < 13*a  ) {  
  
  int i = 20;
  while ( i <= 600 ) {  // outter
    int j = 340;
    while ( j <= 600 ) {  // inner
      fill(i,100,250,100);
      rect(j-20,i-20, 20,20);      
      j = j + 40;
    }
    i = i + 40;
  }

  }
  

 
}



