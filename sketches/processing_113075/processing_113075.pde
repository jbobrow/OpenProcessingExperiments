
size(1000, 700);
background(255, 220, 240);
noStroke();

//body

fill(0, 190, 200);
for (int x = 110; x <= width; x += 250) {
      for (int y = 190; y <= height; y+=210) {
        ellipse(x, y, 50, 90);
        }
        }

for (int x = 85; x <= width; x += 250) {
      for (int y = 185; y <= height; y+=210) {
        rect(x, y, 50, 40);
        }
        }
//shadow
fill(150, 150, 150);
 for (int x = 130; x <= width; x += 250) {
      for (int y = 225; y <= height; y+=210) {
        ellipse(x, y, 90, 30);
        }
        }  

//beard
fill(100, 80, 30);
for (int x = 110; x <= width; x += 250) {
      for (int y = 140; y <= height; y+=210) {
        ellipse(x, y, 40, 90);
        }
        }
        
fill(255);
for (int x = 110; x <= width; x += 250) {
      for (int y = 130; y <= height; y+=210) {
        ellipse(x, y, 38, 87);

    }
    }
//face
fill(0, 200, 210);
for (int x = 110; x <= width; x += 250) {
      for (int y = 114; y <= height; y+=210) {
        ellipse(x, y, 38, 87);
    }
    }
//eyes
fill(255, 220, 240);
 for (int x = 110; x <= width; x += 250) {
      for (int y = 115; y <= height; y+=210) {
        ellipse(x, y, 12, 10);
    }
    }
    fill(255);
     for (int x = 110; x <= width; x += 250) {
      for (int y = 130; y <= height; y+=210) {
        ellipse(x, y, 13, 10);
        }
        }
        fill(255, 0, 0);
     for (int x = 110; x <= width; x += 250) {
      for (int y = 130; y <= height; y+=210) {
        ellipse(x, y, 5, 5);
        }
        }
        
    
//mouth
fill(0);
for (int x = 110; x <= width; x += 250) {
      for (int y = 141; y <= height; y+=210) {
        ellipse(x, y, 10, 12);
        }
        }
fill(0, 200, 210);
for (int x = 110; x <= width; x += 250) {
      for (int y = 140; y <= height; y+=210) {
        ellipse(x, y, 14, 10);
        }
        }
//hair
fill(0);
 for (int x = 120; x <= width; x += 250) {
      for (int y = 103; y <= height; y+=210) {
        ellipse(x, y, 30, 30);
    }
    }
     for (int x = 100; x <= width; x += 250) {
      for (int y = 103; y <= height; y+=210) {
        ellipse(x, y, 30, 30);
    }
    }
//hat
fill(255, 200, 0);
 for (int x = 98; x <= width; x += 250) {
      for (int y = 100; y <= height; y+=210) {
        ellipse(x, y, 30, 30);
    }
    }
 for (int x = 110; x <= width; x += 250) {
      for (int y = 85; y <= height; y+=210) {
        ellipse(x, y, 30, 30);
    }
    }
  
 for (int x = 122; x <= width; x += 250) {
      for (int y = 100; y <= height; y+=210) {
        ellipse(x, y, 30, 30);
    }
    }
