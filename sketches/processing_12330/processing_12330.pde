

/*
void Ellipse (float x, float y, float rotationangle, float quantity ) {  
  float px;
  float py;
  float theta = TWO_PI;
  for(int i=0; i<quantity; i++) { 
    ellipseMode(CORNERS);
    px =  cos(theta)*400;
    py =  sin(theta)*400;
    line( x, y, x+px, y+py);
    theta += rotationangle;
   
  }
}
*/


