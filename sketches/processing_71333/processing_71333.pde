
//HomeWork 3: Flying Magic Cubes of color and doom! 
//By Zach Ruddell
//May have used some example code from learing processing to figure out bouncing.
//But I think I changed it, lost track to be honest. 

int kurtz = 0;
int gatsby = 50;
int speed = gatsby;
//My variables are fictious literary figures, hope thats cool

void setup(){
  size(800,800);
  smooth();
 
}

void draw(){
  background(mouseX,mouseY,gatsby);
  
kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(634,mouseY,mouseX);
  rect(1,kurtz,32,32);
  //1
  
   kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(84,mouseY,mouseX);
  rect(33,kurtz,32,32);
  //2

kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(978,mouseY,mouseX);
  rect(65,kurtz,32,32);
  //3
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(2,mouseY,mouseX);
  rect(97,kurtz,32,32);
  //4
  
kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(kurtz,mouseY,mouseX);
  rect(129,kurtz,32,32);
  //5
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(32,mouseY,mouseX);
  rect(161,kurtz,32,32);
  //6
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(452,mouseY,mouseX);
  rect(193,kurtz,32,32);
  //7
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(734,mouseY,mouseX);
  rect(225,kurtz,32,32);
  //8
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(213,mouseY,mouseX);
  rect(257,kurtz,32,32);
  //9
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(56,mouseY,mouseX);
  rect(289,kurtz,32,32);
  //10
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(365,mouseY,mouseX);
  rect(321,kurtz,32,32);
  //11
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(654,mouseY,mouseX);
  rect(353,kurtz,32,32);
  //12
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(721,mouseY,mouseX);
  rect(385,kurtz,32,32);
  //13
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(123,mouseY,mouseX);
  rect(417,kurtz,32,32);
  //14
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(876,mouseY,mouseX);
  rect(449,kurtz,32,32);
  //15
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(555,mouseY,mouseX);
  rect(481,kurtz,32,32);
  //16
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(143,mouseY,mouseX);
  rect(513,kurtz,32,32);
  //17
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(11,mouseY,mouseX);
  rect(545,kurtz,32,32);
  //18
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(423,mouseY,mouseX);
  rect(577,kurtz,32,32);
  //19
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(789,mouseY,mouseX);
  rect(609,kurtz,32,32);
  //20
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(876,mouseY,mouseX);
  rect(641,kurtz,32,32);
  //21
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(46,mouseY,mouseX);
  rect(673,kurtz,32,32);
  //22
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(169,mouseY,mouseX);
  rect(705,kurtz,32,32);
  //23
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(651,mouseY,mouseX);
  rect(737,kurtz,32,32);
  //24
  
  kurtz = kurtz + speed;
  if ((kurtz > width) || (kurtz <0)) {
    speed = speed * -1;
  }
  if ((kurtz > height) || (kurtz<0));
  
  stroke(0);
  fill(gatsby,mouseY,mouseX);
  rect(769,kurtz,32,32);
  //25
}

