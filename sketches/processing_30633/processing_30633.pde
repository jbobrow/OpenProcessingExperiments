
 size (800,600); 
  background (255); 
  smooth();
    for ( int i = 0; i < 100; i = i +1)
    {
     
        fill(255);
        stroke (random(0,500));
        strokeWeight(random(30)); 
        float radius = random (100); 
        arc ( random(width), random(height/2-30, height/2+30), radius,radius, radians(0), radians ( random(360)) );
     
    }  
    
    

for ( int i = 0; i < 100; i = i +1)
{
 
    fill(255);
    stroke (random(0,255));
    strokeWeight(random(40)); 
    float radius = random ( 300); 
    arc (  random(width/1-50, width/1+50), random(height), radius,radius, radians(0), radians ( random(360)) );

}




  for ( int i = 0; i < 100; i = i +1)
    {
     
        fill(255);
        stroke (random(0,500));
        strokeWeight(random(30)); 
        float radius = random (100); 
        arc ( random(width), random(height/1-30, height/1+30), radius,radius, radians(0), radians ( random(360)) );
     
    }  
    
    
    
    
     for ( int i = 0; i < 100; i = i +1)
    {
     
        fill(255);
        stroke (random(0,500));
        strokeWeight(random(30)); 
        float radius = random (100); 
        arc ( random(width), random(height/9-30, height/9+30), radius,radius, radians(0), radians ( random(360)) );
     
    }  
    
    
    
    
    for ( int i = 0; i < 100; i = i +1)
    {
     
        fill(255);
        stroke (random(0,100));
        strokeWeight(random(10)); 
        float radius = random (200); 
        arc ( random(width), random(height/10-30, height/10+30), radius,radius, radians(0), radians ( random(360)) );
     
    }  
                    
