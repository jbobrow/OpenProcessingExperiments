
//import processing.pdf.*;



size(500,1000); //,PDF,"littleripples.pdf");{
background(0);
stroke(255);
strokeWeight(0.8);
fill(255,20);
smooth();


for(int j=0; j<5; j++) {
  int x=100;
  int y=j*200+100;
  float radius=0.7;
  float expon=2.2;
  //expon is the exponent variable
  
  for(int i=0; i<6; i++){
    float currentRadius=pow(i+6.5,expon)*radius;
    ellipse(x,y,currentRadius,currentRadius);

  

  }
}

for(int j=0; j<5; j++) {
  int x=400;
  int y=j*200+100;
  float radius=0.7;
  float expon=2.2;
  //expon is the exponent variable
  
  for(int i=0; i<6; i++){
    float currentRadius=pow(i+6.5,expon)*radius;
    ellipse(x,y,currentRadius,currentRadius);

 
  }}
  
  for(int j=0; j<5; j++) {
  int x=250;
  int y=j*200+100;
  float radius=0.7;
  float expon=2.2;
  //expon is the exponent variable
  
  for(int i=0; i<6; i++){
    float currentRadius=pow(i+6.5,expon)*radius;
    ellipse(x,y,currentRadius,currentRadius);

 
  }}
   for(int j=0; j<5; j++) {
  int x=50;
  int y=j*200+100;
  float radius=0.7;
  float expon=2.2;
  //expon is the exponent variable
  
  for(int i=0; i<6; i++){
    float currentRadius=pow(i+6.5,expon)*radius;
    ellipse(x,y,currentRadius,currentRadius);

 
  }}
  for(int j=0; j<5; j++) {
  int x=100;
  int y=j*200+100;
  float radius=0.7;
  float expon=2.2;
  //expon is the exponent variable
  
  for(int i=0; i<6; i++){
    float currentRadius=pow(i+6.5,expon)*radius;
    ellipse(x,y,currentRadius,currentRadius);

 
  }}
for(int j=0; j<5; j++) {
  int x=150;
  int y=j*200+100;
  float radius=0.7;
  float expon=2.2;
  //expon is the exponent variable
  
  for(int i=0; i<6; i++){
    float currentRadius=pow(i+6.5,expon)*radius;
    ellipse(x,y,currentRadius,currentRadius);

 
  }}
  for(int j=0; j<5; j++) {
  int x=200;
  int y=j*200+100;
  float radius=0.7;
  float expon=2.2;
  //expon is the exponent variable
  
  for(int i=0; i<6; i++){
    float currentRadius=pow(i+6.5,expon)*radius;
    ellipse(x,y,currentRadius,currentRadius);

 
  }}
  for(int j=0; j<5; j++) {
  int x=250;
  int y=j*200+100;
  float radius=0.7;
  float expon=2.2;
  //expon is the exponent variable
  
  for(int i=0; i<6; i++){
    float currentRadius=pow(i+6.5,expon)*radius;
    ellipse(x,y,currentRadius,currentRadius);

 
  }}
  for(int j=0; j<5; j++) {
  int x=300;
  int y=j*200+100;
  float radius=0.7;
  float expon=2.2;
  //expon is the exponent variable
  
  for(int i=0; i<6; i++){
    float currentRadius=pow(i+6.5,expon)*radius;
    ellipse(x,y,currentRadius,currentRadius);

 
  }}
  for(int j=0; j<5; j++) {
  int x=350;
  int y=j*200+100;
  float radius=0.7;
  float expon=2.2;
  //expon is the exponent variable
  
  for(int i=0; i<6; i++){
    float currentRadius=pow(i+6.5,expon)*radius;
    ellipse(x,y,currentRadius,currentRadius);

 
  }}
  for(int j=0; j<5; j++) {
  int x=400;
  int y=j*200+100;
  float radius=0.7;
  float expon=2.2;
  //expon is the exponent variable
  
  for(int i=0; i<6; i++){
    float currentRadius=pow(i+6.5,expon)*radius;
    ellipse(x,y,currentRadius,currentRadius);

 
  }}
  for(int j=0; j<5; j++) {
  int x=450;
  int y=j*200+100;
  float radius=0.7;
  float expon=2.2;
  //expon is the exponent variable
  
  for(int i=0; i<6; i++){
    float currentRadius=pow(i+6.5,expon)*radius;
    ellipse(x,y,currentRadius,currentRadius);

 
  }}
// println("Finished.");
  //exit();


