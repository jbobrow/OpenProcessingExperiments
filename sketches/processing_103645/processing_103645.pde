
float xPos = 0;

float yPos = 0;

void setup()
{
  size(500, 500);
  background(0);
  //noStroke();
}

void draw() 
 { 
  xPos = mouseX;
  yPos = mouseY;
   
  
  if (mousePressed);
  
  fill(255, 100, 50);
  rect(mouseX, mouseY, 50, 50);
  }
