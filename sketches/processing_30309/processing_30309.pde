
int counter;

void setup() {  //setup function called initially, only once
  size(250,250);
  background(144,122,111);
  colorMode(155);   //set colors to Hue, Saturation, Brightness mode
  counter = 0;
}
void draw(){
text("somto ojoko",111,111);         
textAlign(center);
textSize(150,150);
}

