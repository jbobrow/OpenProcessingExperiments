



void setup() {
  size(600, 600);
  smooth();
  frameRate(20);

  //INITIALIZE
  spermCollection = new ArrayList(); //we can dynamicly include ellements in ArrayList
  
//  for(int i = 0; i < 100; i++) {
//    Ball myBall = new Ball(random(0,width), random(0,height));
//    ballCollection.add(myBall);  //we are adding instance (myBall) to ArrayList
//  }
}

