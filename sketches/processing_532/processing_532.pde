
int x;
int y;

void setup()
{
  size(220, 210);
  //background(0);
  frameRate(25);
}

void draw()
{
  background(0,0,0);
  //prima
  for(x=0; x<=10; x++)
  {
    for(y=61; y<=140; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=11; x<=20; x++)
  {
    for(y=41; y<=60; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
   
    for(y=141; y<=150; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=21; x<=30; x++)
  {
    for(y=31; y<=40; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    
    for(y=141; y<=160; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=31; x<=40; x++)
  {
    for(y=21; y<=30; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=141; y<=190; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=41; x<=50; x++)
  {
    for(y=21; y<=30; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=141; y<=150; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for (y=191; y<=200; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=51; x<=60; x++)
  {
    for(y=11; y<=20; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=141; y<=150; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=200; y<=210; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=61; x<=70; x++)
  {
    for(y=11; y<=20; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=131; y<=140; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=200; y<=210; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=71; x<=80; x++)
  {
    for(y=11; y<=20; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=131; y<=140; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=200; y<=210; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=81; x<=90; x++)
  {
    for(y=1; y<=10; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=131; y<=140; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=200; y<=210; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=91; x<=100; x++)
  {
    for(y=1; y<=10; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=131; y<=170; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=200; y<=210; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=101; x<=110; x++)
  {
    for(y=1; y<=10; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=131; y<=140; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=200; y<=210; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=111; x<=120; x++)
  {
    for(y=1; y<=10; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=131; y<=140; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=200; y<=210; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=121; x<=130; x++)
  {
    for(y=1; y<=10; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=131; y<=170; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=200; y<=210; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=131; x<=140; x++)
  {
    for(y=1; y<=10; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=131; y<=140; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=200; y<=210; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=141; x<=150; x++)
  {
    for(y=11; y<=20; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=131; y<=140; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=200; y<=210; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=151; x<=160; x++)
  {
    for(y=11; y<=20; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=131; y<=140; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=200; y<=210; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=161; x<=170; x++)
  {
    for(y=11; y<=20; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=141; y<=150; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=200; y<=210; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=171; x<=180; x++)
  {
    for(y=21; y<=30; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=141; y<=150; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=191; y<=200; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=181; x<=190; x++)
  {
    for(y=21; y<=30; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=141; y<=190; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=191; x<=200; x++)
  {
    for(y=31; y<=40; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=141; y<=160; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=200; x<=210; x++)
  {
    for(y=41; y<=60; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
    for(y=141; y<=150; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  for(x=211; x<=220; x++)
  {
    for(y=61; y<=140; y++)
    {
      stroke(random(0, 255));
      point(x, y);
    }
  }
  
  //verde
  for(x=101; x<=130; x++)
  {
    for(y=11; y<=20; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  
  for(x=91; x<=140; x++)
  {
    for(y=21; y<=30; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  
  for(x=81; x<=150; x++)
  {
    for(y=31; y<=40; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  for(x=31; x<=40; x++)
  {
    for(y=31; y<=40; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  for(x=181; x<=190; x++)
  {
    for(y=31; y<=40; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  
  for(x=21; x<=200; x++)
  {
    for(y=41; y<=50; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  
  for(x=41; x<=180; x++)
  {
    for(y=51; y<=60; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  
  for(x=51; x<=90; x++)
  {
    for(y=61; y<=70; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  for(x=131; x<=170; x++)
  {
    for(y=61; y<=70; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  
  for(x=51; x<=80; x++)
  {
    for(y=71; y<=110; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  for(x=141; x<=170; x++)
  {
    for(y=71; y<=110; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  
  for(x=41; x<=80; x++)
  {
    for(y=111; y<=120; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  for(x=141; x<=180; x++)
  {
    for(y=111; y<=120; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  
  for(x=11; x<=90; x++)
  {
    for(y=120; y<=131; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  for(x=131; x<=211; x++)
  {
    for(y=120; y<=131; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  
  for(x=11; x<=60; x++)
  {
    for(y=131; y<=140; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
  for(x=161; x<=210; x++)
  {
    for(y=131; y<=140; y++)
    {
      stroke(0, random(0, 255), 0);
      point(x, y);
    }
  }
}


