
// crashCourse processing by Corneel Cannaerts 2014
// points & pixels

// a program is executed one line at a time
// a line starting with // in ingored by the computer 
// and only readable by us humans 
// lines the computer executes ends with a; 

// point() is a function (rightclick > find in reference... to see a description + example)
// the reference contains all syntax processing understands 
// point has two parameters, the x and y coordinate
// the coordinates of the point are passed between () brackets separated by a ,
// the coordinates are expressed in pixels on the screen, top left is 0,0

point(30, 30);
point(85, 20);
point(80, 75);
point(30, 85);


