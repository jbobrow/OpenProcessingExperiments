
/*Drawing circles to a pattern. Answer the questions that follow

Developed by Laura Maye, University of Limerick */

/* Q: Refer to comment underneath all the ellipses in draw()
   Q: Colour the ellipses to a colour of your choice
   Q: Use variables to store the x and y values of each ellipse
*/

void setup()
{
  size(200, 200);
  background(0);
}

void draw()
{
  ellipse(0, 100, 10, 10);
  ellipse(20, 80, 10, 10);
  ellipse(40, 100, 10, 10);
  ellipse(60, 80, 10, 10);
  //... add more ellipses here to finish the pattern!
}
