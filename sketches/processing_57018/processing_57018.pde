
//runs once
void setup(){
  size (800, 800);
  background(157, 219, 178);
  
}
  
  //runs in a loop
  void draw(){
 
    
  
    
    fill (mouseX, mouseY, 50, 75);
    ellipse(mouseX, mouseY, mouseX, mouseY);
    // x1, y1, x2, y2
    
    
  }
  


