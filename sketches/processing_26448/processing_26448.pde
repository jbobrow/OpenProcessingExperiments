
//The Boolean variable button = !button can not exist in draw()
//because it renders the if(button) element of the code false.
//By placing button = !button outside the draw() function, 
//these two true/ false statements can exist independently.


