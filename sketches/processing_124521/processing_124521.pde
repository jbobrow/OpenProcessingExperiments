
void setup() {
  size(200, 200);
  background(255);
  //Above is the background.
}

void draw() {
  stroke(0);
  fill(175);
  rectMode(CENTER);
  rect(mouseX, mouseY, 50, 50);
}
//This above shows the rectangle (how it looks like) and how it is able to move around. 


