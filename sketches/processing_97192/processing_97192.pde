
void setup(){
  size(200,200);
  background (0);
}
void draw(){
  fill(mouseX, 40,150 );
  ellipse(mouseX, mouseY, mouseY, mouseX);
  fill( 45, mouseY,mouseY);
  rect(mouseX, mouseY, mouseY, mouseX);
  stroke(255);
 rect (90,90,20,20);
 {
 stroke(0);
 
if
(mouseX >=90 && mouseY <=110 && mouseX <= 110 && mouseY>=90)
background (0);
}
}
