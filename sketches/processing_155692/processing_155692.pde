


void setup() {  //setup function called initially, only once
  size(500, 500);
  
  colorMode(HSB);   //set colors to Hue, Saturation, Brightnes mode
}

void draw() {  //draw function loops 
rect(0, 0, 500, 500)
rect(20, 30, 322,413);

}
