
size (300, 300);
background (255);
smooth();
 
int rx=20;
for (int x=1; x<=10; x=x+1) {
  for  (int y=1; y<=10; y=y+1) { 
 
  
 triangle (rx, rx, rx+10, rx+20, rx+20, rx);
  
 
}
  }
                
                
