
int counter;

void setup() {  //setup function called initially, only once
  size(400, 300);
  background(150,100,75);  //set background white
  colorMode(HSB);   //set colors to Hue, Saturation, Brightness mode
  counter = 0;
}
void draw(){
textSize(100,150);
text("Ryan",100,150)
}
