


size(510,510);



 for(int x=0; x<=510; x ++){

   stroke(0,0,100+x);
   line(510,x,510,0);
 }





for(int x=0; x<=510; x ++){

   stroke(0,210-x,200+x);
   line(x,510,510,0);
 }
 
 
 
 for(int x=0; x<=510; x ++){

   stroke(0,0,x);
   line(510,x,0,0);
 }


 for(int x=0; x<=510; x ++){

   stroke(0,210-x,200+x);
   line(0,x,510,510);
 }
 
for(int x=0; x<=230; x ++){
stroke(255,255,x,50);
noFill();
ellipse(255,255+x,x,x);
}

for(int x=0; x<=230; x ++){
stroke(200,230,x,50);
noFill();
ellipse(255,255-x,x,x);
}

for(int x=0; x<=230; x ++){
stroke(200,230,x,50);
noFill();
ellipse(255-x,255,x,x);
}

for(int x=0; x<=230; x ++){
stroke(200,230,x,50);
noFill();
ellipse(255+x,255,x,x);
}

for(int x=0; x<=80; x ++){
stroke(210,94,x,240);
noFill();
ellipse(255,255,x,x);
}

for(int x=0; x<=230; x ++){
stroke(200,230,x,50);
noFill();
ellipse(255+x,255+x,x,x);
}

for(int x=0; x<=230; x ++){
stroke(200,230,x,50);
noFill();
ellipse(255-x,255-x,x,x);
}

for(int x=0; x<=230; x ++){
stroke(200,230,x,50);
noFill();
ellipse(255+x,255+x,x,x);
}

for(int x=0; x<=230; x ++){
stroke(200,230,x,50);
noFill();
ellipse(255+x,255-x,x,x);
}

for(int x=0; x<=230; x ++){
stroke(200,230,x,50);
noFill();
ellipse(255-x,255+x,x,x);
}

for(int x=0; x<=230; x ++){
stroke(200,230,x,150);
noFill();
ellipse(255,0,x,x);
}

for(int x=0; x<=230; x ++){
 stroke(0,210-x,200+x,150);
noFill();
ellipse(0,0,x,x);
}

for(int x=0; x<=230; x ++){
 stroke(0,210-x,200+x,150);
noFill();
ellipse(510,0,x,x);
}


for(int x=0; x<=230; x ++){
 stroke(0,210-x,200+x,150);
noFill();
ellipse(510,510,x,x);
}

for(int x=0; x<=230; x ++){
 stroke(0,210-x,200+x,150);
noFill();
ellipse(0,510,x,x);
}

for(int x=0; x<=230; x ++){
stroke(200,230,x,150);
noFill();
ellipse(255,510,x,x);
}

for(int x=0; x<=230; x ++){
stroke(200,230,x,150);
noFill();
ellipse(0,255,x,x);
}

for(int x=0; x<=230; x ++){
stroke(200,230,x,150);
noFill();
ellipse(510,255,x,x);
}


