
int counter;

void setup() {  //setup function called initially, only once
 background (128);
 size(200, 200);
 }
 void draw (){
 
  if(mousePressed == true) { //add some interaction
  background (60,60);
   }
}
