
PImage img;
int lastX, lastY;

void setup()
{
  frameRate(1000);
  size(1200,600);
  background(98,155,250);

 
 
}

int value = 0;
void draw() 
    
{
  if(mousePressed == true)
  {
    line(lastX, lastY, mouseX, mouseY);
    stroke(0);
    strokeWeight(1.7);
   
   
}

import netscape.javascript.*;
 }
    


void mousePressed()
{
  lastX = mouseX;
  lastY = mouseY;
}


void mouseReleased()
{
  
}

