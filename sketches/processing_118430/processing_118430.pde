
int eSize = 3;

size(510, 510);
background(255);




  for(int x = 0; x <= width; x ++){
  stroke(x,200,0,200);
  line(0,0,x,height);
  
  stroke(55,x,0,127);
 line(width,0,x,height);
  }
 for(int y = 0; y <= height; y +=10)    
  for(int x = 0; x <= width; x += 10)   
     ellipse(x, y, eSize, eSize);
  
 noFill();
 for(int i =100; i >= 0; i --){
   stroke(0,0,255-i*2.55,100);
  ellipse(width/2,height/2,i,i);
 
 }
  
   noFill();
 for(int i =100; i >= 0; i --){
   stroke(0,0,i*2.55,100);
  ellipse(width,height,i,i);
 
 }
  
   noFill();
 for(int i =100; i >= 0; i --){
   stroke(0,255,255-i*2.55,100);
  ellipse(width/2,height/2,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(86,0,25-i*2.55,100);
  ellipse(width/4,height/2,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(32,53,55-i*2.55,100);
  ellipse(width/2,height/3,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(55,45,0-i*2.55,100);
  ellipse(width/1,height/2,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(39,0,55-i*2.55,100);
  ellipse(width/3,height/7,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(30,0,25-i*2.55,100);
  ellipse(width/6,height/2,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(255,0,255-i*2.55,100);
  ellipse(width/6,height/3,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(90,33,85-i*2.55,100);
  ellipse(width/1,height/1,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(91,213,20-i*2.55,100);
  ellipse(width/5,height/3,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(1,255,0-i*2.55,100);
  ellipse(width/3,height/7,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(255,0,25-i*2.55,100);
  ellipse(width/1,height/8,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(99,75,5-i*2.55,100);
  ellipse(width/5,height/9,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(74,200,25-i*2.55,100);
  ellipse(width/4,height/4,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(100,75,55-i*2.55,100);
  ellipse(width/1,height/5,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(1,9,5-i*2.55,100);
  ellipse(width/9,height/3,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(6,1,5-i*2.55,100);
  ellipse(width/6,height/6,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(34,98,255-i*2.55,100);
  ellipse(width/5,height/5,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(0,0,25-i*2.55,100);
  ellipse(width/2,height/1,i,i);
 
 }
 
  noFill();
 for(int i =100; i >= 0; i --){
   stroke(80,50,5-i*2.55,100);
  ellipse(width/3,height/7,i,i);
 
 }


